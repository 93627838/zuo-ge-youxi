#pragma once
#include <string>

// ============================================================
//  UI.h —— 控制台交互层(战斗系统的 I/O 模块)
//
//  说明:本文件是兼容实现。战斗系统的原作者上传时遗漏了 UI 模块,
//        但 Combat / Item / shop / Game 四个文件都依赖 UI::Print 等接口。
//        这里的接口是按那些文件中的实际调用点反推出来的,行为保持一致。
//        若日后拿到原作者的正式 UI.h/UI.cpp,可直接整份替换本文件。
// ============================================================
namespace UI {

    void Clear();                            // 清屏并把光标归位到左上角
    void Print(const std::string& text);     // 输出一行文本
    void Pause();                            // 等待玩家按一次回车
    int  GetInt();                           // 读一个整数(非法输入返回 0)
    std::string GetLine();                   // 读一整行文本

}
